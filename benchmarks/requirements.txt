# Pinned client libraries for the Celery side of the benchmark. Rustvello comes
# from this checkout (`make develop`), so both sides are measured at known versions.
celery==5.6.3
kombu==5.6.2
redis==8.1.0
SQLAlchemy==2.1.1
psycopg[binary]==3.3.6
psutil==7.2.2
