# Agent skill examples and scripts: short, readable, runnable files (Python 3.9+).
# Kept outside skills/rustvello so it is not copied with the skill.
extend = "../pyproject.toml"
line-length = 120
target-version = "py39"

[lint]
select = ["E", "W", "F", "I", "B", "UP", "C4"]

[lint.isort]
lines-between-types = 1
